# 하루치 핏 — 남은 작업 로드맵

작성일: 2026-09-07
상태: **살아있는 문서** — 작업이 끝날 때마다 갱신한다
선행 문서: `specs/2026-09-02-haruchi-fit-product-spec.md` · `specs/2026-09-02-haruchi-fit-architecture.md`

---

## 이 문서의 역할

제품 스펙이 정한 **14개 화면·기능을 무엇을 어떤 순서로 만들지**만 확정한다.
파일 단위 설계는 **각 작업 착수 직전에 별도 플랜 문서로** 쓴다.

> **왜 상세 플랜을 미리 쓰지 않나** — 플랜의 값어치는 *그 시점의 코드와 검증 결과*를 반영하는
> 데서 나온다. 실제로 두 번 겪었다.
>
> - 타깃 골격 플랜의 `WorkoutCore 세그먼트 확장` 항목은 `HKWorkoutActivity` 검증 후 **통째로 삭제**됐다
> - W2 플랜의 핵심(HealthKit 사후 삭제)은 **현재 `stopWorkout()` 구현을 읽어야만** 나오는 결론이었다
>
> 미리 쓴 플랜은 앞의 두어 개만 살아남는다. 이 문서는 그 영향을 받지 않는 층위만 담는다.

---

## 완료된 것

| 작업 | 결과 |
|---|---|
| 타깃 3개 생성 · CI 등록 | PR #6 |
| `HKWorkoutActivity` 실기기 검증 | 이종 전환 불가 확정 → 폴백 (`b51f1b6`) |
| 스파이크 코드 제거 | PR #7 |
| W1 모드 라벨 · 전환 행 (D-M1) | PR #8 |
| 세그먼트 SwiftData 저장 | PR #9 |

---

## 남은 14개

### 의존 관계

```
[기반]
  잔디 집계 ─────┬──▶ 02 홈 · 03a 달력 · 05 통계
  HealthKit import ┘

[워치]  W2 ──▶ W0 ──▶ WC

[iOS]   탭 셸 + 디자인 토큰 ──▶ 03b 목록 ──▶ 04 상세 ──▶ 06 공유

[독립]  CloudKit 엔타이틀먼트 · 08 수동 기록 · 01 온보딩 · 07 설정
```

### Phase 1 — 워치 세션을 닫는다

| # | 작업 | 스펙 | 비고 |
|---|---|---|---|
| 1 | **W2 요약** | W2절 | **플랜 완료** — `2026-09-07-haruchi-fit-w2-summary.md`. 자동 저장이라는 임시 상태를 닫는다 |
| 2 | W0 홈 토글 | W0절 | 시작 유형(근력/유산소) 토글. 마지막 선택 기억. 세그먼트 초기값을 정하므로 W2 뒤 |
| 3 | WC 컴플리케이션 | WC절 · D-M4 | 세션 상태만. 집계 값 없음. **로컬 스냅샷 스토어** 필요 (골프 `RoundSnapshotStore` 대응) |

Phase 1 이 끝나면 **폰을 한 번도 안 열어도 흐름 A(해피패스)가 완결**된다 (제품 스펙 8절).

### Phase 2 — 데이터 기반

| # | 작업 | 스펙 | 비고 |
|---|---|---|---|
| 4 | 잔디 집계 | 5절 · D3 · D4 | 하루 1칸, 여러 세션 시간 합산. 농도 4단계(30/60/90분). **일별 집계 캐시**가 핵심 |
| 5 | HealthKit import | 4.1 · 4.2 · D-M5 | `HKAnchoredObjectQuery` 증분. 포그라운드 진입 시에만. **근력/유산소 매핑 표 확정** — 한번 정하면 과거 잔디가 바뀌므로 신중히 |

둘 다 화면이 없어 **눈으로 확인할 수단이 필요하다.** 임시 화면을 쓸지, Phase 3 을 먼저 할지는
착수 시점에 정한다.

### Phase 3 — iOS 뼈대와 기록

| # | 작업 | 스펙 | 비고 |
|---|---|---|---|
| 6 | 탭 셸 + 디자인 토큰 | 3절 · 7절 | 탭 3개(홈·기록·통계). 지금 `ContentView` 는 **저장 확인용 임시 리스트**다 |
| 7 | 03b 기록 목록 | 03b절 | 임시 리스트를 대체한다 |
| 8 | 04 기록 상세 | 04절 · D1 · D6 | 하프 시트. **부위 태깅(멀티) · 메모** — 모델엔 필드가 이미 있다 |
| 9 | 06 공유 | 06절 · D-M2 | `WorkoutShareUI` **그대로 사용, 수정 없음**. 04 상세에서 진입 |

### Phase 4 — 잔디를 쓰는 화면

| # | 작업 | 스펙 | 비고 |
|---|---|---|---|
| 10 | 02 홈 대시보드 | 02절 | 잔디 + 오늘 요약 + 최근 기록 2개 |
| 11 | 03a 기록 달력 | 03a절 | 날짜 농도는 잔디와 **같은 규칙** |
| 12 | 05 통계 | 05절 · D2 · D7 | 연도 아카이브. 세션 카운트 기준. 전환 횟수 표시 안 함 |

### Phase 5 — 주변부

| # | 작업 | 스펙 | 비고 |
|---|---|---|---|
| 13 | 07 설정 | 07절 | 잔디 기준 시간↔칼로리 전환 포함 → Phase 2 이후 |
| 14 | 08 수동 기록 | 08절 · 4.3 | 권한 거부 시에도 앱이 완결되게 하는 fallback |
| 15 | 01 온보딩 | 01절 | HealthKit 권한 요청. 지금은 `.task` 로 무조건 요청 중 |

### 언제든 — 순서 무관

| 작업 | 비고 |
|---|---|
| CloudKit 엔타이틀먼트 | **프로비저닝 작업(사용자)**. 안 해도 `PersistenceContainerFactory` 가 로컬 폴백하므로 급하지 않다. 기기 간 동기화가 필요해지는 시점에 |

---

## 순서를 이렇게 잡은 이유

**워치를 먼저 닫는다** — 제품 스펙이 *"폰을 한 번도 안 열어도 완결"* 을 흐름 A 로 삼았다.
그 경로가 먼저 서야 앱이 최소한으로 쓸모 있어진다. 지금은 자동 저장이라 그 경로가 미완이다.

**잔디를 iOS 화면보다 먼저** — 홈·달력·통계 셋이 같은 집계를 공유한다. 화면부터 만들면
셋이 각자 계산하게 되고, 나중에 캐시를 넣을 때 세 곳을 고쳐야 한다.

**HealthKit import 를 늦추지 않는다** — 매핑 표를 한번 정하면 *"사용자의 과거 잔디가 달라지므로
이후 변경에 주의해야 한다"* (4.2). 잔디 화면이 나오기 **전에** 확정하는 편이 안전하다.

---

## 작업 중 지킬 것

- **각 작업 착수 직전에 상세 플랜을 쓴다.** 이 문서의 항목 하나가 플랜 문서 하나에 대응한다
- 실기기 검증이 필요한 것 — **햅틱**(전부), **워치↔폰 전송**, **인스타 딥링크**(06)
- Xcode 수작업이 필요한 것 — 새 폴더를 타깃에 붙일 때. 규약은 저장소 루트 `docs/superpowers/specs/2026-09-03-xcode-target-conventions.md`
- 패키지(`Packages/YJKit`)를 건드리면 **CI 가 3개 앱을 전부 빌드**한다. 기존 시그니처는 바꾸지 않는다
