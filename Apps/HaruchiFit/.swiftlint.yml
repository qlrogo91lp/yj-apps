parent_config: ../../.swiftlint.yml

# included 는 이 앱 디렉터리 기준 상대 경로다. 앱 폴더에서 swiftlint 를 실행한다.
# Shared/ 는 아직 없다 — 컴플리케이션이 읽을 스냅샷 타입이 생기면 만든다.
# swiftlint 는 없는 경로를 조용히 무시하므로 미리 적어둔다.
included:
  - iOSApp
  - WatchApp
  - ComplicationApp
  - Shared
