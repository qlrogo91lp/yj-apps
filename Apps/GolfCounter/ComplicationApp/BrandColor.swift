import SwiftUI

extension Color {
    /// 앱 아이콘 그라디언트 기준색 (GolfCounter.icon의 automatic-gradient 값, #00520A)
    static let brand = Color(red: 0, green: 0.3216, blue: 0.0392)
    /// 라운드 진행 중 배경색 — 짙은 초록 배경과 구분되도록 시스템 오렌지 사용
    static let brandActive = Color.orange
}
